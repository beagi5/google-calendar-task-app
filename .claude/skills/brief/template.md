# ブリーフ: <タスク名>

日付: <YYYY-MM-DD>
実行役: sidekick (Sonnet 5) / 外部 / lead 自身

## タスク

<何を達成するか。1〜3行。背景ではなく達成状態を書く>

## 対象ファイル

- `path/to/file.js` — <ここで何をするか>

※ ここに無いファイルは変更しない。必要が生じたら PUSHBACK で報告する。

## 制約（変えてはいけないもの）

- <既存の公開APIのシグネチャ / 既存の動作 / 依存の追加禁止 など>

## 成功基準

コマンドで確かめられる形で書く。

- [ ] <例: `npm run build` がエラーなく完了する>
- [ ] <例: `grep -c "oauth2Client" server/index.js` が 1 になる>
- [ ] <例: 既存の /api/tasks の応答形式が変わっていない>

## 範囲外

- <やらないことを明示。スコープ逸脱の防止線>

## 反論してほしい点

- <ブリーフの前提で自信がない箇所。ここが違っていたら止めてほしい、と書く>
