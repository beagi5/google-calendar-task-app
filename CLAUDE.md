# google-calendar-task-app

Google カレンダー連携のタスク管理アプリ。React (CRA) + Express + Passport (Google OAuth)。

- フロント: `src/` — CRA。`src/pages/HomePage.js` が中心（903行）
- サーバ: `server/index.js` — Express 単一ファイル（660行）。OAuth、カレンダーCRUD、タスクCRUD
- 秘密情報: `server/.env`（git管理外）。詳細は `SETUP.md`
- 常駐: PM2 (`ecosystem.config.js`)。スマホからは Tailscale 経由

## 起動・確認

```
npm run build && node server/index.js   # http://localhost:3001
```

---

# 作業プロトコル（Fusion 型委譲）

このリポジトリでは lead / sidekick / verifier の役割分担で作業する。
根拠と設計意図は `.claude/DELEGATION.md`。

## lead（このセッションの私）が持つもの、手放さないもの

lead は**意図の保持者**であり、以下は絶対に委譲しない。

- 何を作るか・何を直すかの判断
- **計画のための探索**。どのファイルが効くかの見極めは計画そのものを形づくるので、
  lead が自分で読む。「実装のための探索」だけが委譲可能。
- 成功基準（success criteria）の執筆
- レビューと受け入れの最終判断

## 委譲するかどうかの判断基準

**「安くしたいか」ではなく「このタスクは仕様で切れるか」で決める。**

| 委譲する | 委譲しない |
|---|---|
| 仕様が確定していて、機械的に検査できる | 仕様が探索の途中で変わる |
| 変更範囲がファイル単位で列挙できる | 状態が連続していて途中で文脈が切れない |
| 例: 定型的な書き換え、テスト追加、ファイル横断の一括修正 | 例: 対話的なデバッグ、原因不明の不具合の切り分け、設計判断 |

判断がつかないときは**委譲しない**。ブリーフが書けないタスクは、まだ仕様が
固まっていないという意味なので、lead が仕様を固める方が先。

## ブリーフの原則

- sidekick には**会話履歴を渡さない**。ブリーフ・結果・フィードバックだけを交換する。
- sidekick は毎回コールドスタートする。**ブリーフに書いていないことは伝わらない。**
  これは制約ではなく設計。ブリーフが唯一の契約面になる。
- ブリーフは `.claude/briefs/<YYYYMMDD>-<slug>.md` に**ファイルとして残す**。
  外部モデル（Gemini Pro 等）への検証依頼にそのまま使うため。
- 詳細度は相手に合わせる。Sonnet 5 sidekick は強いので、**実装の詳細は任せて
  制約と成功基準を厚く書く**。手順を一行ずつ書くのは micromanage であり、
  往復とトークンを増やして総コストを上げる。

## 反論（pushback）

sidekick には**反論を許可する**。強い実行役に反論させると lead の計画ミスを捕まえられる。
ブリーフの前提が実際のコードと食い違っていたら、実装を進める前に報告させる。

## 検証

lead は sidekick の**中間ツール結果を読まない**。ブリーフに書いた成功基準と、
出てきた差分だけを見て判定する。実装者の思考過程を見ると、その合理化に引きずられる。

独立性をさらに上げたいときは `scripts/handoff.sh` で仕様＋差分を書き出し、
外部モデル（Gemini Pro）に違反だけ列挙させる。詳細は `.claude/DELEGATION.md`。
